{
  "name": "nati-beniyas-speedy-adventure",
  "private": true,
  "version": "1.8.0",
  "type": "module",
  "description": "A browser platform-adventure game built with Phaser 3.",
  "scripts": {
    "build": "esbuild js/main.js --bundle --outfile=dist/bundle.js --target=es2017 --format=iife"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
